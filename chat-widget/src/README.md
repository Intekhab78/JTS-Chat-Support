# Chat Widget Package

The live widget is currently served by the backend at `/chat-widget.js` so managers can use a single deployment URL.

This package is reserved for a future standalone widget build pipeline if you want versioned assets later.
